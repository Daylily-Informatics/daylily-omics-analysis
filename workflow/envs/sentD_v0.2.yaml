channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - samtools
  - jemalloc
  - htslib
  - pigz
  - ipython
  - pip  # Specify pip as a dependency first
  - pip:
      - sentieon-cli==1.6.3
