# QuicK-mer2 configuration assets

This directory holds project-specific configuration files required by the
QuicK-mer2 copy-number analysis workflow.  Populate the TSV files referenced in
`config/day_profiles/*/templates/rule_config.yaml` with the appropriate catalogs
for your sequencing project before executing the rule.
