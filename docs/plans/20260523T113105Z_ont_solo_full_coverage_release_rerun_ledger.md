# ONT Solo Full-Coverage Release And Rerun Ledger

## Summary

Release the minimal DayOA changes needed for the successful full-coverage HG003 ONT solo run, tag a new version, then launch a fresh `day-clone -t <tag>` ONT solo run with alignstats and corrected HG003 concordance manifest paths.

## Gate 0 Inventory

- Repo: `/Users/jmajor/projects/daylily/daylily-omics-analysis`
- Branch: `main`
- Remote: `origin git@github.com:Daylily-Informatics/daylily-omics-analysis.git`
- Baseline dirty files before release edits: `workflow/rules/sent_hybrid_ilmn_ont_modular.refactored.smk`, `workflow/rules/sent_hybrid_ilmn_ont_modular.smk`
- Latest existing release tag: `1.0.18`
- Successful diagnostic run: `hg003a_ont_snv_alignstats_1018_191_retain_tmp_v4`, `exit_code=0`, completed `2026-05-23T11:22:59Z`
- Successful ONT evidence: `sentieon-cli exit code: 0`, elapsed 11 minutes on `m7i.metal-48xl`, Slurm queue empty after completion
- Concordance skip root cause: staged `samples.tsv` points at `/data/staged_sample_data/.../concordance_data`, which is missing; actual staged truth data exists at `/fsx/data/staged_sample_data/.../concordance_data`
- Release `1.0.19`: commit `363d161e62853cba099f8ad0e784163144139c15`, pushed to `origin/main` and tag `1.0.19`.
- Fresh ONT rerun from `1.0.19`: `hg003a_ont_solo_fullcov_1019_concordance`, started `2026-05-23T11:36:58Z`, failed `exit_code=1` at `2026-05-23T11:51:52Z`.
- Fresh hiomr stdlib-fix dry-run from `1.0.19`: `hg003_hiomr_5x5x_1019_stdlibfix2_dryrun`, `exit_code=0`, completed `2026-05-23T11:43:15Z`.
- Fresh hiomr stdlib-fix real 5x run from `1.0.19`: `hg003_hiomr_5x5x_1019_stdlibfix2`, started `2026-05-23T11:45:58Z`, entered `sentdhiomr_sr_align` jobs without failing at `importlib_resources`.
- Local source validation after `1.0.19`: `pytest -q tests/test_ont_fastq_contracts.py tests/test_snakemake_parser_contracts.py tests/test_workflow_target_aliases.py` -> 11 passed; `pytest -q tests/test_slurm_profile.py` -> 1 passed; `git diff --check` clean.
- No destructive AWS actions authorized or required.

## Rows

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| SRC-001 | DayOA source | Lower Slurm ONT `sentdont` full-coverage thread request to the successful 191-thread setting. | SUCCESS | feature_implementation | Gate 1 | orchestrator | `config/day_profiles/slurm/templates/rule_config.yaml` now sets `sentdont.threads=191`, `sentdont.use_threads=191`; successful live run used the same values. | | Source config updated. |
| SRC-002 | Hybrid diagnostics | Preserve explicit hybrid-select environment logging for `hiom` and `hiomr` debugging without changing import behavior. | SUCCESS | feature_implementation | Gate 1 | orchestrator | Release `1.0.19` logged `PATH`, `CONDA_PREFIX`, `which python`, `sys.executable`, `importlib_resources`, and resolved `hybrid_select.py`; later source work is tracked by `HIOMR-002`. | | Diagnostic logging was released in `1.0.19`; stdlib import rewrite supersedes only the import behavior. |
| MANIFEST-001 | HG003 manifests | Fix fresh-run HG003 `samples.tsv` concordance paths to `/fsx/data/staged_sample_data/.../concordance_data` and keep positive-control fields set. | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Created `/fsx/analysis_results/johnm/staged_manifests/remote_stage_20260522T203135Z_hg003_concordance_fsx_1019`; dry-run `hg003a_ont_solo_fullcov_1019_concordance_dryrun` saw `clinvar_genes`, `giabHC`, `giabHC_x_clinvar_genes`, `hg38`, and `hg38_m_giabHC`. | | Corrected manifest-only stage is in use; original staged data was not mutated. |
| REL-001 | Release | Commit, push, tag next non-`v` semver version, and push tag. | SUCCESS | feature_implementation | Gate 5 | orchestrator | Commit `363d161e62853cba099f8ad0e784163144139c15`; tag `1.0.19`; `git ls-remote origin main refs/tags/1.0.19` confirmed both point to `363d161`. | | `1.0.19` released and pushed. |
| RUN-001 | Fresh ONT run | Launch fresh `day-clone -t <new tag>` full-coverage ONT solo run including alignstats and concordance using corrected manifests. | ATTEMPTING_BUGFIX | feature_implementation | Gate 5 | orchestrator | Dry-run `hg003a_ont_solo_fullcov_1019_concordance_dryrun` completed `exit_code=0`; real run `hg003a_ont_solo_fullcov_1019_concordance` failed `sent_snv_ont` with `exit_code=1`. Rule log shows `sentieon-cli exit code: 1`. | Sentieon CLI deleted its own temp subdirectory too early: `Failed to create tabix file /dev/shm/.../out_diploid_phased_unphased.vcf.gz.tbi: No such file or directory`, `RepeatModel: Failed open output file /dev/shm/.../out_repeat.model: No such file or directory`, then `FileNotFoundError` for `shutil.rmtree(tmp_dir_str)`. The `1.0.19` run did not include `--retain_tmpdir`. | Bugfix in progress: keep `--retain_tmpdir` in the Sentieon CLI call while leaving outer rule TMPDIR cleanup as the default. |
| HIOMR-001 | Hybrid follow-up | Report current `hiomr` target alias and defer focused debug until ONT release/rerun is launched. | SUCCESS | feature_implementation | Gate 5 | orchestrator | Target alias used was `produce_sentdhiomr_snv_vcf`; failing downstream rule was `sentdhiomr_hybrid_select`. | | User asked which `sentdhiomr` rule was specified; answer provided in chat. |
| HIOMR-002 | Hybrid source | Replace undeclared `importlib_resources` backport dependency with Python 3.10 stdlib `importlib.resources` anywhere modular hybrid rules locate `hybrid_select.py`, `hybrid_anno.py`, or `trimalt.py`. | SUCCESS | feature_implementation | Gate 5 | orchestrator | Local source changed across all `workflow/rules/*hybrid*modular*.smk`; `rg -n "importlib_resources" workflow/rules/*hybrid*modular*.smk` returns no hits; `tests/test_snakemake_parser_contracts.py` now enforces the no-backport contract; focused tests passed. | | Source fix ready for release/tag. |
| HIOMR-003 | Hybrid dry-run | Start from fresh `day-clone -t 1.0.19`, apply stdlib import patch in-clone, and dry-run `produce_snv_concordances produce_sentdhiomr_sv produce_sentdhiomr_snv_vcf`. | SUCCESS | feature_implementation | Gate 5 | orchestrator | `hg003_hiomr_5x5x_1019_stdlibfix2_dryrun` completed `exit_code=0` at `2026-05-23T11:43:15Z`; previous `stdlibfix_dryrun` failed only because `rg` was unavailable on the headnode. | | Fresh patched dry-run succeeded. |
| HIOMR-004 | Hybrid 5x live run | Run 5x ILMN plus 5x ONT hiomr test from fresh `day-clone -t 1.0.19` with the stdlib import patch. | BLOCKED | feature_implementation | Gate 5 | orchestrator | `hg003_hiomr_5x5x_1019_stdlibfix2` started `2026-05-23T11:45:58Z`; Slurm is empty; workflow log shows failures in `sentdhiomr_hybrid_select` jobs 190 and 232 after the fatal `HYBRID_SELECT=$(python -c "from importlib.resources ...")` line was already stdlib-based. | The stdlib import issue is fixed; the next hiomr blocker is inside the hybrid-select pipeline command (`sentieon pyexec "$HYBRID_SELECT" | bcftools view | bcftools query | bedtools slop`). Per-rule log inspection is pending. | Continue hiomr debugging separately; do not block ONT solo release. |
| ONT-RETAIN-001 | ONT source | Make Sentieon CLI `--retain_tmpdir` a permanent production flag for ONT `sentdont`; keep outer rule TMPDIR cleanup by default unless `sentdont.keep_tmp_dirs=true` is explicitly configured. | SUCCESS | feature_implementation | Gate 5 | orchestrator | `workflow/rules/sent_snv_ont.smk` passes `--retain_tmpdir` to `sentieon-cli dnascope-longread`; local and Slurm profile templates set `sentdont.keep_tmp_dirs=false`; the trap retains the outer `$TMPDIR` only when that rule-level config is explicitly true; tests passed. | | Source fix implemented; next step is release/tag and a fresh `day-clone -t <new tag>` rerun. |
