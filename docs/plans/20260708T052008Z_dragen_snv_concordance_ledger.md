# DRAGEN SNV Concordance Ledger

Controlling request: formalize DRAGEN so `produce_snv_concordances` can run against explicit DRAGEN-coded outputs, without changing the existing Ubuntu `slurm` path. Clarified DRAGEN codes: aligner `drbwa`, pangenome SNV caller `drgpg`, output root `align/drbwa/snv/drgpg/`.

Ledger path: `docs/plans/20260708T052008Z_dragen_snv_concordance_ledger.md`

## Gate 0 Baseline

- DayOA repo: `/Users/jmajor/projects/lsmc/dragen-fix-20260708/daylily-omics-analysis`
- DayOA branch: `codex/dragen-headnode-launch-fix`
- DayOA status: `## codex/dragen-headnode-launch-fix...origin/codex/dragen-headnode-launch-fix`, clean
- DYEC repo: `/Users/jmajor/projects/lsmc/dragen-fix-20260708/daylily-ephemeral-cluster`
- DYEC branch: `codex/dragen-headnode-launch-fix`
- DYEC status: unrelated dirty files remain in `daylily_ec/cli.py`, `tests/test_cli_registry_v2.py`, and generated TSVs under `docs/plans/20260707T144453Z_dyec_tests_command_catalog_sample_logs/illumina_pangenome_snv/`
- Baseline sweep: `rg -n "DAYLILY_DRAGEN|produce_snv_concordances|sentpg_dragen|slurm_rhel|rtg_vcfeval|sent_aln_sort_snv" workflow config tests bin | wc -l` -> `103`
- Key source surfaces inspected: `workflow/rules/rtg_vcfeval.smk`, `workflow/rules/common.smk`, `workflow/rules/sent_aln_sort_snv.smk`, `config/day_profiles/slurm/templates/rule_config.yaml`, `bin/day_activate`, `bin/day_run`
- Live validation target: existing DYEC DRAGEN cluster `dragen-fix5-20260708`, profile `lsmc`, region `us-west-2`, only through `dy-r` in a persistent `ubuntu` tmux/login-shell pane.
- Assumptions: keep the existing `slurm`/Ubuntu profile behavior unchanged; put RHEL/DRAGEN partition differences in a new explicit `slurm_rhel` profile; no destructive AWS or Slurm admin actions.

## Control Ledger

| ID | Area/Repo | Requirement/Surface | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| DRG-CONC-001 | DayOA profile | Add explicit `slurm_rhel` day profile for DRAGEN/RHEL clusters with f2 `dragen` partition routing for pangenome and concordance work, while leaving `slurm` unchanged. | SUCCESS | feature_implementation | Gate 1 | Codex | Added `config/day_profiles/slurm_rhel/templates/*`; `pytest tests/test_slurm_profile.py tests/test_slurm_caller_partitions.py tests/test_pangenome_kitchensink_contracts.py tests/test_workflow_target_aliases.py tests/test_sentieon_model_bundle_config.py -q` -> `35 passed`. |  | `slurm_rhel` profile exists, uses `DAY_PROFILE=slurm_rhel`, default `partition=dragen`, and `dragen_pangenome` plus RTG sections route to `dragen`; Ubuntu `slurm` retains normal v8 partitions. |
| DRG-CONC-002 | DayOA workflow | Replace the mistaken `sent/spmd/sentpg` DRAGEN interpretation with explicit DRAGEN-coded `drbwa/drgpg` output routing. | SUCCESS | feature_implementation | Gate 1 | Codex | Added `dragen_aln_sort_snv` and `produce_drgpg_vcf` outputs under `align/drbwa/snv/drgpg/`; `rtg_vcfeval.smk` resolves `drbwa/drgpg` inputs to that no-deduper output path; focused tests -> `35 passed`. | Earlier implementation reused `sentpg`; user clarified DRAGEN must have distinct coded aligner/caller names. | `produce_drgpg_snv_vcf produce_snv_concordances` is now the explicit DRAGEN-coded path; `produce_sentpg_snv_vcf` remains the Sentieon pangenome path. |
| DRG-CONC-003 | DayOA workflow | Avoid broad rule changes that would alter the Ubuntu `slurm` concordance path for `sentd`, `deep19`, `sentpg`, and other existing callers. | SUCCESS | contract_test | Gate 1 | Codex | Removed `rtg_vcfeval.enable_sentpg_dragen_concordance`; `drgpg` is `explicit` in `config/workflow_target_aliases.tsv`, not `current`; focused tests -> `35 passed`; catalog/variant routing sweep -> `37 passed`. |  | Default Ubuntu `slurm` profile keeps existing RTG partitions and generic input paths; `produce_all_snv_vcf` does not include `drgpg`. |
| DRG-CONC-004 | DayOA tests | Add focused tests proving `slurm_rhel` resources and DRAGEN `drbwa/drgpg` routing without relying on compatibility fallback behavior. | SUCCESS | contract_test | Gate 1 | Codex | Updated assertions in `tests/test_slurm_profile.py`, `tests/test_slurm_caller_partitions.py`, `tests/test_pangenome_kitchensink_contracts.py`, `tests/test_workflow_target_aliases.py`, and `tests/test_sentieon_model_bundle_config.py`; focused tests -> `35 passed`; broader targeted sweep with `PYTHONPATH=.` -> `37 passed`. |  | Tests cover explicit `drgpg` selector behavior, `drbwa/drgpg` output paths, `slurm_rhel` partition routing, and exclusion from all/current SNV expansion. |
| DRG-CONC-005 | Live validation | Validate with `dy-r` from a persistent headnode shell that `produce_sentpg_snv_vcf produce_snv_concordances` can run and produce RTG concordance outputs on the DRAGEN f2 spot path. | CANCELED | live_validation | Gate 5 | Codex | Headnode tmux `dragen_concordance_20260708T053030Z`; Slurm job `3` submitted for the now-rejected `sent_aln_sort_snv`/`sentpg` route, then canceled with `dyec workflow stop --cancel-slurm-jobs`; helper reported `killed_tmux_session: true`, `scancelled_job_ids: ["3"]`, and `slurm_jobs_after: []`; follow-up `dyec headnode jobs` showed an empty queue. | User clarified the validation was proving the wrong contract: DRAGEN must not be hidden under the Sentieon `sentpg` path. | Canceled before continuing; no cluster teardown and no FSx cleanup performed. |
| DRG-CONC-006 | Code validation | Validate the corrected `drbwa/drgpg` implementation locally before any live rerun. | SUCCESS | contract_test | Gate 1 | Codex | `pytest tests/test_slurm_profile.py tests/test_slurm_caller_partitions.py tests/test_pangenome_kitchensink_contracts.py tests/test_workflow_target_aliases.py tests/test_sentieon_model_bundle_config.py -q` -> `35 passed`; `PYTHONPATH=. pytest tests/test_workflow_catalog.py tests/test_multiqc_qc_targets.py::test_variant_qc_and_annotation_summaries_are_wired tests/test_multiqc_sample_identifiers.py::test_rtg_vcfeval_requests_explicit_memory tests/test_pangenome_ug_sharding.py tests/test_sentdug_specialty_callers.py tests/test_complete_genomics_sentieon.py -q` -> `37 passed`. |  | Corrected implementation is locally validated; no live `drgpg` execution has been run yet. |
