# Dynamic DPPL Memory And Partition Ordering Execution Ledger

Date: 2026-06-19T00:35:33Z

## Gate 0: Inventory Freeze

- Controlling ledger: `docs/plans/20260619T003533Z_dynamic_resources_partition_ledger.md`
- Repo: `/Users/jmajor/projects/lsmc/daylily-omics-analysis`
- Branch/status: `jem-dev...origin/jem-dev`, clean before implementation.
- Required instructions read: `/Users/jmajor/projects/lsmc/AGENTS.md`, `/Users/jmajor/projects/lsmc/daylily-omics-analysis/AGENTS.md`, `/Users/jmajor/.codex/AGENTS-HOW-TO-RUN-DAYOA.md`, `/Users/jmajor/.codex/docs/plan-ledger-workflow.md`.
- Active config surfaces: `config/day_profiles/slurm/templates/rule_config.yaml`, `config/day_profiles/local/templates/rule_config.yaml`, `config/day_profiles/slurm/templates/config.yaml`.
- Active helper/code surfaces: `workflow/rules/common.smk`, `daylily_omics_analysis/slurm/spot_partition_order.py`, primary compute rule files under `workflow/rules/`.
- Existing tests to update/run: `tests/test_spot_partition_order.py`, `tests/test_slurm_caller_partitions.py`, `tests/test_multiqc_qc_targets.py`; add focused dynamic resource helper tests.
- Benchmark evidence: local benchmark summaries show DPPL/dmd markdup RSS p95 about 451 GiB and max about 470 GiB. The initial Slurm formula is floor 512000 MB, 6144 MB/GiB input BAM, cap 800000 MB, rounded up to 1000 MB.
- Live-system limit: no headnode/AWS profile was provided in this implementation turn. Live `dy-r` dry-run and live Slurm/AWS permission validation cannot be marked `SUCCESS` locally; row `PART-003`/`DRYRUN-001` must remain `BLOCKED` unless a valid interactive `ubuntu` headnode session is supplied.
- DayOA execution contract: do not invoke raw `snakemake`; dry-run validation must use `source dyoainit`, `dy-a slurm <genome_build>`, then `dy-r ... -n` in a persistent interactive `ubuntu` tmux/login shell.

## Progress Accounting

- Current readiness: live slim validation bugfix in progress after compute-node reparse exposed an IAM boundary and live DAG construction showed repeated partition-cost cache eviction.
- Basis: implementation, config, static tests, unit tests, local compile checks, branch deploy, headnode clones through `dynamic_partition_slim_20260619_r5`, fixture staging, DRA FASTQ existence checks, live metadata refresh, `PARTITION_MAGIC=0` proof, and local-profile proof are complete; user supplied live target `AWS_PROFILE=lsmc`, `region=us-west-2`, cluster `tstpartition` on 2026-06-19.
- Remaining work: push the partition-cache accumulation bugfix, reclone/retry on `tstpartition`, run slim DRA-backed Doppelmark validation through `dy-r`, and run broader dry-runs.
- Local blocker evidence captured 2026-06-19T01:03:41Z: `daylily-ec` missing, `dyec` missing, and local AWS credentials unavailable.
- Headnode validation packet: `docs/plans/20260619T010341Z_dynamic_resources_headnode_validation_packet.md`.

## 2026-06-19 Slim Validation Gate Update

- Target: `AWS_PROFILE=lsmc`, `region=us-west-2`, cluster `tstpartition`.
- Read-only cluster preflight: `daylily-ec cluster-info --profile lsmc --region us-west-2` showed `tstpartition CREATE_COMPLETE`; `daylily-ec headnode jobs --profile lsmc --region us-west-2 --cluster tstpartition` showed only the queue header.
- Deployment branch: `codex/dynamic-partition-slim-validation`.
- Initial local deploy commit: `c9ad945` on `codex/dynamic-partition-slim-validation`.
- Analysis/workdir: `/fsx/analysis_results/tstpartition/dynamic_partition_slim_20260619/daylily-omics-analysis`.
- Source data: `.test_data/data/stress_tests/ilmn/hg003/3x/{samples.tsv,units.tsv}` copied to `config/`, with FASTQ paths under `/fsx/references/genomic_data/organism_reads_slim/`.
- Command scope: slim catalog rows that exercise `produce_dmd_dedup_cram` and dynamic partition resources. Exclude `complete_genomics_mgi_snv_concordance` because catalog metadata records it as blocked on unverified CG mate pairs.
- Execution contract: no raw `snakemake`; headnode workflow commands must run through `dy-r` in persistent `ubuntu` tmux after separate `source dyoainit` and `dy-a ...` commands.
- Final active checkout before compute-node bugfix: `/fsx/analysis_results/tstpartition/dynamic_partition_slim_20260619_r4/daylily-omics-analysis` at `f2c0404`.
- Live metadata success in r4: `DAY_PROFILE=slurm AWS_REGION=us-west-2 python -m daylily_omics_analysis.slurm.spot_partition_order i384nvme,i192nvme` produced `i384nvme: median_usd_per_vcpu_hr=0.025810937500` and `i192nvme: median_usd_per_vcpu_hr=0.027160546875`; cache stat `R4_METADATA 1781870517 1686 /home/ubuntu/.config/dayoa/partition_costs.log`.
- Live slim run attempt in r4: exact minimal command failed during DAG build on missing upstream `sent.sort.bam`; a targeted `dy-r produce_sent_align -p -k -j 20 --rerun-triggers mtime` submitted Slurm job `1` with `--partition=i384nvme,i192nvme` and Slurm allocated it on `i384nvme`.
- Compute-node failure evidence: job `1` failed before writing the rule log because compute-node workflow reparse called live EC2 metadata and the compute fleet role lacks `ec2:DescribeInstances`.
- Headnode ParallelCluster metadata evidence: `/opt/parallelcluster/shared/cluster-config.yaml` contains `Scheduling.SlurmQueues[].Name` and `ComputeResources[].Instances[].InstanceType`; existing cost helper `/home/ubuntu/projects/daylily-ephemeral-cluster/bin/calcuate_spotprice_for_cluster_yaml.py` wraps `daylily_ec.aws.spot_pricing`, but that cluster-creation library has fallback spot prices and is not reused for this hard-fail validation helper.
- r5 pre-submit metadata retry: checkout `/fsx/analysis_results/tstpartition/dynamic_partition_slim_20260619_r5/daylily-omics-analysis` at `323063b`; live helper rebuilt cache `R5_METADATA 1781872282 1686` and preserved the same `i384nvme,i192nvme` order; `dy-r produce_sent_align ...` was interrupted before Slurm submission after repeated read-only metadata expansion, and `squeue` stayed empty.
- r6 pre-submit metadata retry: checkout `/fsx/analysis_results/tstpartition/dynamic_partition_slim_20260619_r6/daylily-omics-analysis` at `d20be53`; live helper rebuilt cache `R6_METADATA 1781872866 1686` from cluster-config members and spot pricing; `dy-r produce_sent_align ...` was interrupted before Slurm submission when cache row count stayed at 3 after later partition requests, proving fresh cache rows were being overwritten instead of accumulated.

## Rows

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| G0-001 | Ledger | Record inventory, repo state, surfaces, assumptions, and live-system limits before code edits. | SUCCESS | plan_amendment | Gate 0 | Orchestrator | This Gate 0 section. `git status --short --branch` was clean. |  | Inventory frozen before implementation. |
| MEM-001 | DPPL Memory | Add Snakemake-visible DPPL memory helper and slurm/local config keys. | SUCCESS | feature_implementation | Gate 1 | Agent A | `daylily_omics_analysis/workflow_resources.py`; `workflow/rules/common.smk`; slurm/local `doppelmark.dynamic_mem_*` keys. |  | Slurm dynamic defaults: floor 512000, per GiB 6144, cap 800000, round 1000. Local profile disables dynamic sizing. |
| MEM-002 | DPPL Memory | Update `doppelmark_dups.resources.mem_mb` to use the helper. | SUCCESS | feature_implementation | Gate 1 | Agent A | `workflow/rules/doppel_mrkdups.smk` uses `mem_mb=derive_doppelmark_mem_mb`. |  | Helper fails hard for missing/unreadable/empty BAM and malformed dynamic memory config. |
| PART-001 | Partition Costing | Generalize cached live Slurm/AWS partition ordering with 30 minute cache, locking, and atomic write. | SUCCESS | feature_implementation | Gate 1 | Agent B | `daylily_omics_analysis/slurm/spot_partition_order.py`; `python -m py_compile ...` passed. |  | Cache path `~/.config/dayoa/partition_costs.log`; lock path `.lock`; TSV header matches plan; no static config fallback remains. |
| PART-002 | Rule Integration | Wire `derive_partition_order` into primary alignment, dedup, and SNV compute rules only. | SUCCESS | feature_implementation | Gate 1 | Agent C | Alignment/dedup/SNV primary rule files updated; helper exclusions pinned by `tests/test_dynamic_resource_helpers.py`. |  | Sort/index/concat/chunkdir/target helpers remain on configured partitions. |
| PART-003 | Live Metadata | Validate live headnode can read cluster partition metadata and AWS pricing permissions. | SUCCESS | contract_test | Gate 5 | Agent B | Headnode tmux session `dynamic_partition_slim_20260619` on `tstpartition`; `squeue -o '%i  %P  %C  %t  %N  %c  %T  %m  %M  %D  %j'` showed only the header. r5 helper run after `dy-a slurm hg38_broad`: `DAY_PROFILE=slurm AWS_REGION=us-west-2 python -m daylily_omics_analysis.slurm.spot_partition_order i384nvme,i192nvme` -> `i384nvme: median_usd_per_vcpu_hr=0.025810937500` and `i192nvme: median_usd_per_vcpu_hr=0.027160546875`; cache stat `R5_METADATA 1781872282 1686 /home/ubuntu/.config/dayoa/partition_costs.log`. | Powered-down ParallelCluster cloud nodes expose `NodeAddr=NodeName`, `NodeHostName=NodeName`, `State=IDLE+CLOUD+POWERED_DOWN`, and no `InstanceId`; generated `fleet-config.json` has compute resource pools; the first fix requested `ec2:DescribeSubnets`, which headnode IAM lacks; r5 DAG construction showed repeated Slurm node resolution is slow across broader rule imports. | Final local bugfix reads `/opt/parallelcluster/shared/cluster-config.yaml` for partition-to-instance-type mapping and still uses allowed live regional `describe-spot-price-history` plus `describe-instance-types`; no static pricing fallback used. |
| TEST-001 | Tests | Unit-test DPPL sizing floors, caps, rounding, pass-through, and hard failures. | SUCCESS | contract_test | Gate 5 | Agent D | `python -m pytest tests/test_dynamic_resource_helpers.py -q` -> 10 passed. |  | Covered pass-through, floor, rounding, cap, missing input, bad config. |
| TEST-002 | Tests | Unit-test cache freshness, stale refresh, accumulation, locking path, malformed log failure, missing partition failure, `PARTITION_MAGIC=0`, compute-node passthrough, and cluster-config metadata. | SUCCESS | contract_test | Gate 5 | Agent D | `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_slurm_caller_partitions.py tests/test_multiqc_qc_targets.py -q` -> 48 passed after adding the powered-down ParallelCluster regression case, `SLURM_JOB_ID` passthrough assertion, `/opt/parallelcluster/shared/cluster-config.yaml` metadata-source regression, and fresh-cache accumulation regression. |  | Covered local/`PARTITION_MAGIC=0`/`SLURM_JOB_ID` pass-through, stale refresh, fresh-cache reuse, missing-partition accumulation, lock calls, EC2 private-IP/private-DNS node address resolution, ParallelCluster fleet-config metadata for powered-down nodes, cluster-config queue/instance parsing without Slurm node lookup, and malformed cache with missing live metadata. |
| TEST-003 | Tests | Static tests assert primary rule integration and helper-rule non-integration. | SUCCESS | contract_test | Gate 5 | Agent D | `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_slurm_caller_partitions.py tests/test_multiqc_qc_targets.py -q` -> 46 passed. |  | Static assertions cover representative alignment, dedup, SNV primary rules, helper exclusions, Snakefile repo-root import path before shared rule includes, dry-run-only missing BAM placeholder wiring, local/slurm prep input resource-key parity, and local concordance resource-key coverage. |
| DRYRUN-001 | DayOA Dry Run | Validate through `dy-r ... -n` in interactive headnode tmux, never raw `snakemake`. | SUCCESS | contract_test | Gate 5 | Agent D | Contract doc read; no raw `snakemake` invoked. In persistent `ubuntu` tmux, `PARTITION_MAGIC=0 AWS_REGION=us-west-2 dy-r produce_sent_align produce_dmd_dedup_cram produce_sentd_snv_vcf produce_snv_concordances produce_alignstats -p -k -j 20 --rerun-triggers mtime -n` completed with return code 0 after dry-run placeholder bugfix; local-profile minimal dry-run also completed with return code 0 in r4. | Earlier blocked state was missing interactive headnode session plus dry-run resource callable issues. | DayOA contract satisfied for the disablement/local proof surfaces; broader catalog dry-runs remain tracked under `SLIM-007`. |
| SLIM-001 | Deployment | Commit/push the exact dynamic-resource source state to `codex/dynamic-partition-slim-validation` for headnode cloning. | ATTEMPTING_BUGFIX | plan_amendment | Gate 5 | Codex | Initial commit `c9ad945` pushed and cloned; powered-down metadata commit `d9727ab` pulled on headnode; local IAM-adjusted regional spot-history bugfix passed tests and is pending commit/push. | Powered-down cloud nodes and the headnode IAM surface were not fully covered by the initial live metadata helper. |  |
| SLIM-002 | Headnode Setup | Clone branch on `tstpartition`, initialize a persistent single-pane tmux session, and stage HG003 slim samples/units from repo fixtures. | SUCCESS | legitimate_safety_handling | Gate 5 | Codex | SSM connected as `ubuntu` to `i-0657bfbc8ec3c41ae`; tmux session `dynamic_partition_slim_20260619`; latest active workdir before compute-node bugfix `/fsx/analysis_results/tstpartition/dynamic_partition_slim_20260619_r4/daylily-omics-analysis` at `f2c0404`; copied `.test_data/data/stress_tests/ilmn/hg003/3x/{samples.tsv,units.tsv}` to `config/`; both staged files have 2 lines. |  | Verified DRA FASTQs exist: `HG003_3x_R1.fastq.gz` 2.2G and `HG003_3x_R2.fastq.gz` 2.3G under `/fsx/references/genomic_data/organism_reads_slim/fastq/H_sapiens/giab/NovaSeqX_WHGS_TruSeqPF_HG002-007/downsampled/`. |
| SLIM-003 | Live Metadata | Refresh `~/.config/dayoa/partition_costs.log` from live cluster/AWS metadata for `i384nvme,i192nvme`. | SUCCESS | contract_test | Gate 5 | Codex | After separate `source dyoainit` and `dy-a slurm hg38_broad`, `rm -f ~/.config/dayoa/partition_costs.log*`; r5 helper run produced live costs for `i384nvme` and `i192nvme`; cache stat `R5_METADATA 1781872282 1686 /home/ubuntu/.config/dayoa/partition_costs.log`; TSV rows contain `availability_zone regional`. | Initial helper did not support powered-down ParallelCluster fleet metadata; first fix required an IAM action not allowed on the headnode role; r5 showed broader DAG imports repeatedly resolving node names. | Live cluster/AWS-derived cost cache exists and orders `i384nvme` before `i192nvme`; next retry will use cluster-config YAML for queue members rather than Slurm node-name inference. |
| SLIM-004 | Disablement | Prove `PARTITION_MAGIC=0` preserves configured partition order and does not refresh the cache during a minimal `dy-r ... -n`. | SUCCESS | contract_test | Gate 5 | Codex | `PARTITION_MAGIC=0 AWS_REGION=us-west-2 dy-r produce_sent_align produce_dmd_dedup_cram produce_sentd_snv_vcf produce_snv_concordances produce_alignstats -p -k -j 20 --rerun-triggers mtime -n` completed with return code 0 after dry-run placeholder bugfix; cache stat stayed `BEFORE_MAGIC_RETRY3 1781869500 1692` -> `AFTER_MAGIC_RETRY3 1781869500 1692`. | Earlier failures came from missing repo-root importability and missing upstream BAM resource-callable behavior during dry-run. | `PARTITION_MAGIC=0` disables dynamic partition sorting, preserves configured order, and does not refresh the partition cache. |
| SLIM-005 | Local Profile | Prove `dy-a local hg38_broad` preserves configured partition order and does not hit Slurm/AWS metadata. | SUCCESS | contract_test | Gate 5 | Codex | In r4 at `f2c0404`, `dy-a local hg38_broad` succeeded; direct helper probe `DAY_PROFILE=local AWS_REGION=us-west-2 python -c 'from daylily_omics_analysis.slurm.spot_partition_order import derive_partition_order; print(derive_partition_order("i384nvme,i192nvme"))'` printed `i384nvme,i192nvme`; local minimal `dy-r ... -n` completed with return code 0; cache stat stayed `R4_BEFORE_LOCAL 1781869500 1692` -> `R4_AFTER_LOCAL 1781869500 1692`. | Local profile template was missing explicit resource keys required by `prep_input_sample_files`, `roche_downsample_bam`, `prep_for_concordance_check`, and `rtg_vcfstats` rule surfaces. | Local profile preserves configured partition order and does not refresh or require Slurm/AWS metadata. |
| SLIM-006 | Live Slim Run | Run minimal slim `illumina_snv_alignstats` equivalent through `dy-r` without `-n` to exercise Doppelmark dynamic memory against produced BAM input. | ATTEMPTING_BUGFIX | bugfix_attempt | Gate 5 | Codex | Exact minimal command failed during DAG build because real execution hard-fails until upstream `sent.sort.bam` exists; targeted r4 `AWS_REGION=us-west-2 dy-r produce_sent_align -p -k -j 20 --rerun-triggers mtime` submitted Slurm job `1` with `--partition=i384nvme,i192nvme`, allocated on `i384nvme`, then failed before rule log output. Slurm `.err` shows `dynamic partition ordering failed: ... ec2:DescribeInstances ... not authorized` from compute-node workflow reparse. r5 targeted alignment retry at `323063b` was interrupted before Slurm submission after repeated metadata expansion; r6 targeted alignment retry at `d20be53` was interrupted before Slurm submission after cache eviction was observed; `squeue` stayed empty for both interrupted retries. | Compute fleet role lacks `ec2:DescribeInstances`; Snakemake reparses workflow inside allocated jobs where partition ordering can no longer influence scheduling; broad rule imports repeatedly trigger dynamic ordering for configured partitions; cache refresh overwrote previous fresh rows instead of accumulating missing partition costs. | Local bugfix preserves configured order when `SLURM_JOB_ID` is set, reads `/opt/parallelcluster/shared/cluster-config.yaml` for partition members during headnode pricing, and accumulates fresh cache rows; commit/push and fresh headnode retry pending. |
| SLIM-007 | Broader Dry Runs | Run broader successful Doppelmark catalog dry-runs for `illumina_snv_alignstats_relatedness_vep_multiqc` and `illumina_hg002_kitchensink_multiqc`. | OPEN | contract_test | Gate 5 | Codex | Planned `dy-r` equivalents of the catalog rows; CG/MGI row excluded due catalog-blocked mate-pair validation. |  |  |
| SLIM-008 | Debug Attempts | If live validation fails from code behavior, record at least one concrete bugfix attempt with local tests before retry. | ATTEMPTING_BUGFIX | bugfix_attempt | Gate 5 | Codex | Patched `daylily_omics_analysis/slurm/spot_partition_order.py` to read generated ParallelCluster fleet config for powered-down cloud nodes when live EC2 instance resolution is empty, then revised it to avoid `DescribeSubnets` and price powered-down fleet pools from allowed live regional spot history; added `test_partition_order_uses_parallelcluster_fleet_config_for_powered_down_nodes` and asserted no `describe-subnets` call. Patched `workflow/Snakefile` to insert repo root on `sys.path` before shared rule includes; added `test_snakefile_adds_repo_root_to_import_path_before_rule_includes`. Patched `workflow_resources.derive_doppelmark_mem_mb` with explicit `allow_missing_input` and wired `common.smk` to use it only for Snakemake dry-run flags; real execution still hard-fails on missing BAM. Patched local profile `prep_input_sample_files`, `roche_downsample_bam`, `prep_for_concordance_check`, and `rtg_vcfstats` resource keys to match rule contracts. Patched `derive_partition_order` to preserve configured order when `SLURM_JOB_ID` is set so compute-node workflow reparse does not call EC2 metadata; added an assertion that this path leaves the cache untouched. Patched the metadata source to parse `/opt/parallelcluster/shared/cluster-config.yaml` for `SlurmQueues[].Name` and `ComputeResources[].Instances[].InstanceType`; added a regression proving no `sinfo`, `scontrol`, or `describe-instances` calls are made when the YAML is present. Patched cache refresh to retain fresh existing rows and price only missing requested partitions; added a regression proving a fresh cache row is preserved and not re-resolved. Verification: `python -m py_compile daylily_omics_analysis/slurm/spot_partition_order.py daylily_omics_analysis/workflow_resources.py daylily_omics_analysis/workflow_catalog.py` -> passed; `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_slurm_caller_partitions.py tests/test_multiqc_qc_targets.py -q` -> 48 passed; `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_workflow_catalog.py -q` -> 29 passed. | Initial live metadata helper only handled active EC2 instances or private address resolution; first powered-down fix requested `ec2:DescribeSubnets`, which the headnode role lacks; Snakemake did not inherit repo-root importability for local helper modules; dry-run missing upstream BAM was converted into fatal resource-callable errors; local profile template lacked explicit prep/Roche/concordance resource keys; compute fleet role lacks EC2 metadata permissions during worker reparse; broader DAG construction was slow when repeatedly resolving Slurm node names; cache refresh evicted prior fresh rows. | Commit/push and live retry pending. |

## Verification

- `python -m pytest tests/test_dynamic_resource_helpers.py -q` -> 12 passed.
- `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_slurm_caller_partitions.py tests/test_multiqc_qc_targets.py -q` -> 48 passed.
- `python -m pytest tests/test_dynamic_resource_helpers.py tests/test_workflow_catalog.py -q` -> 29 passed.
- `python -m py_compile daylily_omics_analysis/slurm/spot_partition_order.py daylily_omics_analysis/workflow_resources.py daylily_omics_analysis/workflow_catalog.py` -> passed.
- `rg -n "update_rule_config|partition_strategy|partition_instance_catalog|spot_price_runtime|--rule-config|dynamic BWA-MEM2A" .` -> only historical release-note mentions remain.
