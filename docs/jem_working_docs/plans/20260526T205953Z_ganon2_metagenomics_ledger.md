# Ganon2 Unmapped Metagenomics Ledger

Date: 2026-05-26T20:59:53Z

## Gate 0 Inventory

- Repo: `/Users/jmajor/projects/daylily/daylily-omics-analysis`
- Branch: `main`
- Initial status: `git status --short --branch` -> `## main...origin/main`
- Controlling request: add a Ganon2 rule parallel to the existing unmapped-read Kraken2 screen, review required configuration, and check Daylily ephemeral cluster supporting buckets for the Kraken database.
- Guardrails: no inferred database paths, no fallback classifier behavior, no default AWS profile. Live S3 inspection requires a user-provided non-default AWS profile.
- Source sweep: `rg -n -i "ganon|ganon2|metagen" workflow config tests docs README.md AGENTS.md pyproject.toml` -> no active Ganon2 implementation; active metagenomics surface is `workflow/rules/unmapped_metagenomics.smk`.
- Baseline tests: `eval "$(conda shell.zsh hook)" && conda activate DAY-EC && python -m pytest -q tests/test_unmapped_metagenomics.py tests/test_multiqc_staging_contracts.py tests/test_multiqc_qc_targets.py` -> `32 passed in 0.20s`.
- External reference review: Ganon2 official docs describe `ganon classify --db-prefix ... --output-prefix ... --single-reads/--paired-reads ... --threads ...`; classify emits `.tre`, `.rep`, and optional `.one` / `.all` files. DayOA will use explicit `ganon2_db_prefixes` config and produce parser-ready custom TSV evidence.
- User reference guidance: first-version Ganon2 QC databases should emphasize broad representative/refseq/reference coverage across major taxa, selected viral/vector/adaptor references, explicit host/control bins, and optional GTDB bacterial/archaeal representative layers. Do not treat full NT, all GenBank, all strains, or all eukaryotes as the first QC build.
- User Kraken/Ganon database guidance: prefer RefSeq reference genomes, then RefSeq representatives, then GenBank representatives; keep one assembly per species by default; include all RefSeq viral genomes, organelles/plasmids where useful, UniVec/PhiX/adapters/vectors, the exact upstream human reference, and a frozen manifest with accession, taxid, source, assembly level, checksums, and build date.
- Sourmash amendment: user requested a secondary fingerprint branch using `sourmash gather`, with Kraken2, Ganon2, and sourmash all runnable together through the kitchen-sink/final MultiQC opt-in or the explicit `produce_metagenomics` target.
- Sourmash reference review: sourmash docs support `sourmash sketch dna -p ... abund` for unassembled reads, and `sourmash gather <query.sig> <database...> -o <csv>` with `--threshold-bp`. DayOA will require explicit sourmash database collection paths and sketch parameters; no database path, k-size, scaled value, moltype, or threshold is inferred.

## Ledger

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| GANON2-001 | Workflow | Add a Snakemake 7-compatible Ganon2 rule for pass-QC human-unmapped reads. | SUCCESS | feature_implementation | Gate 1 | Codex | `workflow/rules/unmapped_metagenomics.smk` adds `unmapped_metagenomics_ganon2_quick` and `produce_unmapped_metagenomics_ganon2_quick`; `python -m pytest -q tests` -> `203 passed`. |  | Ganon2 rule is implemented as an explicit optional target. |
| GANON2-002 | Config | Require explicit Ganon2 config and fail hard for missing DB prefix/resources/read mode. | SUCCESS | config_or_startup_contract | Gate 2 | Codex | Ganon2 config requires non-empty `unmapped_metagenomics.ganon2_db_prefixes`, `threads >= 16`, `mem_mb`, `partition`, and explicit `read_limit: all`; shell checks each prefix for `.hibf`/`.ibf` plus `.tax`. |  | No Ganon2 DB path is inferred. Multiple explicit prefixes support representative all-life-ish layers. |
| GANON2-003 | Summary | Emit deterministic `_mqc.tsv` summary preserving classifier/database/provenance fields. | SUCCESS | feature_implementation | Gate 1 | Codex | `workflow/scripts/summarize_unmapped_ganon2.py`; parser tests cover `.tre`, `.rep`, read counts, top taxon, classifier, database prefix list, and sample provenance. |  | Ganon2 summary emits parser-ready custom TSV evidence. |
| GANON2-004 | MultiQC | Keep focused Ganon2 MultiQC output optional and do not enable it in routine final MultiQC by default. | SUCCESS | active_product_contract | Gate 3 | Codex | Focused `unmapped_metagenomics_ganon2_multiqc` emits `reports/unmapped_metagenomics_ganon2.multiqc.html`; final MultiQC requires `multiqc_qc.enable_tools=['unmapped_metagenomics_ganon2']`. |  | Routine final MultiQC remains unchanged unless explicitly enabled. |
| GANON2-005 | Tests | Add focused tests for Ganon2 rule wiring, config, env, parser, and summary output. | SUCCESS | contract_test | Gate 5 | Codex | `tests/test_unmapped_metagenomics.py`, `tests/test_multiqc_staging_contracts.py`, `tests/test_plan_qc_benchmarking_contracts.py`, `tests/test_tool_catalog_docs.py`; focused suite -> `43 passed`; full suite -> `203 passed`. |  | Focused and full local tests pass. |
| GANON2-006 | Docs | Review and document required Ganon2/Kraken config surfaces. | SUCCESS | historical_docs_only | Gate 5 | Codex | `docs/ops/multiqc_qc_targets.md` documents explicit config, no inferred DBs, and representative-first Ganon2 QC reference guidance; `docs/catalog_of_tools.md` adds Ganon2 inventory row. |  | Docs capture the requested Ganon2 reference strategy and config surface. |
| GANON2-007 | DYEC Buckets | Check Daylily ephemeral cluster supporting buckets for Kraken database objects. | BLOCKED | legitimate_safety_handling | Gate 5 | Codex | Local DayOA bucket ledger shows split buckets `lsmc-dayoa-references-usw2`, `lsmc-dayoa-runtime-assets-usw2`, and public `daylily-dayoa-references-usw2`; anonymous read-only S3 spot checks found zero objects at public `daylily-dayoa-references-usw2/{,data/}tool_specific_resources/kraken2/`; LSMC buckets returned `AccessDenied`; old `daylily-omics-analysis-references-public/data/tool_specific_resources/kraken2/` requires requester-pays authentication. | Missing required non-default AWS profile for authenticated LSMC/requester-pays S3 listing. | Unblock with a non-default AWS profile name and region, then run authenticated read-only `aws s3 ls` over the LSMC split buckets and old requester-pays source. |
| SOURMASH-001 | Workflow | Add a Snakemake 7-compatible sourmash gather secondary fingerprint rule for pass-QC human-unmapped reads. | SUCCESS | feature_implementation | Gate 1 | Codex | `workflow/rules/unmapped_metagenomics.smk` adds `unmapped_metagenomics_sourmash_gather` and `produce_unmapped_metagenomics_sourmash_gather`; focused suite -> `42 passed`. |  | sourmash sketch/gather branch is implemented as an explicit optional target. |
| SOURMASH-002 | Config | Require explicit sourmash DB collections and sketch parameters. | SUCCESS | config_or_startup_contract | Gate 2 | Codex | Sourmash config requires non-empty `unmapped_metagenomics.sourmash_databases`, `sourmash_ksize`, `sourmash_scaled`, `sourmash_moltype: DNA`, `sourmash_threshold_bp`, `threads >= 16`, `mem_mb`, `partition`, and explicit `read_limit: all`; shell checks every configured collection path. |  | No sourmash database path or sketch parameter is inferred. |
| SOURMASH-003 | Summary | Emit deterministic `_mqc.tsv` summary preserving sourmash evidence and provenance. | SUCCESS | feature_implementation | Gate 1 | Codex | `workflow/scripts/summarize_unmapped_sourmash.py`; parser tests cover FASTQ counts, `.sig`, gather CSV, sketch params, weighted fraction, top match, classifier, database list, and sample provenance. |  | sourmash summary emits parser-ready custom TSV evidence. |
| SOURMASH-004 | MultiQC | Wire sourmash into focused MultiQC and final/kitchen-sink staging only when explicitly enabled. | SUCCESS | active_product_contract | Gate 3 | Codex | Focused `unmapped_metagenomics_sourmash_multiqc` emits `reports/unmapped_metagenomics_sourmash.multiqc.html`; final MultiQC accepts either `multiqc_qc.enable_tools=['unmapped_metagenomics_sourmash']` or umbrella `['metagenomics']`. |  | Routine final MultiQC remains gated; the umbrella gate intentionally pulls Kraken2, Ganon2, and sourmash together. |
| SOURMASH-005 | Aggregate target | Add `produce_metagenomics` target that runs Kraken2, Ganon2, and sourmash evidence together. | SUCCESS | feature_implementation | Gate 1 | Codex | `workflow/rules/unmapped_metagenomics.smk` adds `produce_metagenomics` with all three focused MultiQC reports as inputs; docs and contract tests reference the target. |  | Operators can run all three with `dy-r produce_metagenomics ...` when the explicit DB config is present. |
| SOURMASH-006 | Tests and docs | Cover sourmash rule wiring, parser, staging, env, docs, catalog, and umbrella gating. | SUCCESS | contract_test | Gate 5 | Codex | `tests/test_unmapped_metagenomics.py`, `tests/test_multiqc_staging_contracts.py`, `tests/test_plan_qc_benchmarking_contracts.py`, `docs/ops/multiqc_qc_targets.md`, `docs/catalog_of_tools.md`; focused suite -> `42 passed`. |  | Local contract coverage proves the sourmash branch and all-three target shape. |

## Terminal Report

- Status counts: `SUCCESS=12`, `BLOCKED=1`, `OPEN=0`, `IN_PROGRESS=0`, `ATTEMPTING_BUGFIX=0`, `FAIL=0`.
- Validation: Ganon2 pass: `python -m pytest -q tests` -> `203 passed`; focused metagenomics/MultiQC/docs suite -> `43 passed`; sourmash focused pass: `python -m pytest -q tests/test_unmapped_metagenomics.py tests/test_multiqc_staging_contracts.py tests/test_plan_qc_benchmarking_contracts.py tests/test_multiqc_qc_targets.py` -> `42 passed`; final full suite: `python -m pytest -q tests` -> `207 passed`; `python -m py_compile workflow/scripts/summarize_unmapped_metagenomics.py workflow/scripts/summarize_unmapped_ganon2.py workflow/scripts/summarize_unmapped_sourmash.py workflow/scripts/stage_multiqc_inputs.py` -> pass; `git diff --check` -> pass.
- Snakemake dry-run was not run locally because `snakemake` is not on `PATH` in the Mac `DAY-EC` environment (`command -v snakemake && snakemake --version` -> exit 1).
- Objective status: code, tests, docs, and local/anonymous bucket reconnaissance are complete. Authenticated S3 confirmation of Kraken database objects in DYEC/LSMC supporting buckets remains blocked on a user-provided non-default AWS profile.
