

The CLI does not have a direct “download from headnode” subcommand, but it does have the supported FSx export path. I’m going to export just the two requested FSx subdirectories to the backing S3 repository, then sync those two exported prefixes into local ./blahgo.

